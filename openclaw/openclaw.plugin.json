{
  "id": "rubato",
  "name": "Rubato",
  "description": "Rubato model-state monitor: captures every model call (Estimate/Thinking/Done/Error) and publishes each state as an MQTT message to the desk device.",
  "activation": {
    "onStartup": true
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": true
  }
}
