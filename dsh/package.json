{
  "name": "rubato-dsh",
  "version": "1.0.0",
  "description": "Rubato (musical term: stolen time) model-state monitor for DeepSeek Harness: captures thinking/generating/done states per model call, estimates duration locally (feature-based kNN), and publishes to EMQX (rubato/<deviceId>/state). Host-only plugin.",
  "type": "module",
  "main": "lib/index.js",
  "exports": {
    ".": {
      "default": "./lib/index.js"
    },
    "./mqtt": {
      "default": "./lib/mqtt.js"
    },
    "./package.json": "./package.json"
  },
  "license": "GPL-3.0"
}
